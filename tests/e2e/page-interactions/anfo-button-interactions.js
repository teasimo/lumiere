// Automatisch erzeugte Button-Interactions fuer das Anfo-Modul.
// Jeder Eintrag ist ein ungepruefter KI-Vorschlag und muss fachlich validiert werden.

export const anfoButtonInteractions = [
  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-filter-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-filter-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-filter-action-10-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-filter-action-10-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-filter-action-11-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-filter-action-11-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-filter-action-12-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-filter-action-12-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-filter-action-13-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-filter-action-13-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-filter-action-14-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-filter-action-14-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-filter-action-15-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-filter-action-15-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-filter-action-16-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-filter-action-16-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-filter-action-17-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-filter-action-17-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-filter-action-18-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-filter-action-18-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-filter-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-filter-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-filter-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-filter-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-filter-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-filter-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-filter-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-filter-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-filter-action-6-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-filter-action-6-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-filter-action-7-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-filter-action-7-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-filter-action-8-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-filter-action-8-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-filter-action-9-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-filter-action-9-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-inline-editor-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-inline-editor-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-inline-editor-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-inline-editor-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-inline-editor-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-inline-editor-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-inline-editor-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-inline-editor-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-listview-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-listview-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-listview-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-listview-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-listview-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-listview-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-listview-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-listview-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-listview-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-listview-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-problem-add-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-problem-add-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-problem-add-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-problem-add-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-problem-list-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-problem-list-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-problem-list-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-problem-list-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-problem-list-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-problem-list-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-problem-list-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-problem-list-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-problem-list-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-problem-list-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-search-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-search-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-search-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-search-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-search-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-search-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-select-dialog-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-select-dialog-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-select-dialog-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-select-dialog-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-select-dialog-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-select-dialog-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-statusview-add-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-statusview-add-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-statusview-add-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-statusview-add-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-view-open-search-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-view-open-search-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-view-open-local-edit-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-view-open-local-edit-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-view-save-local-edit-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-view-save-local-edit-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-view-cancel-local-edit-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-view-cancel-local-edit-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-view-import-from-confluence-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-view-import-from-confluence-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-view-open-specification-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-view-open-specification-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-view-open-defect-create-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-view-open-defect-create-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-view-copy-link-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-view-copy-link-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-view-open-status-add-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-view-open-status-add-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-view-status-shortcut-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-view-status-shortcut-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-view-open-ziel-meta-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-view-open-ziel-meta-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-view-action-8-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-view-action-8-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-view-status-delete-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-view-status-delete-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungen-multiselect-open-status-add-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungen-multiselect-open-status-add-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungen-multiselect-clear-selection-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungen-multiselect-clear-selection-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungen-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungen-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-add-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-add-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-add-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-add-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-add-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-add-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-filter-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-filter-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-filter-action-10-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-filter-action-10-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-filter-action-11-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-filter-action-11-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-filter-action-12-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-filter-action-12-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-filter-action-13-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-filter-action-13-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-filter-action-14-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-filter-action-14-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-filter-action-15-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-filter-action-15-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-filter-action-16-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-filter-action-16-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-filter-action-17-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-filter-action-17-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-filter-action-18-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-filter-action-18-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-filter-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-filter-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-filter-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-filter-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-filter-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-filter-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-filter-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-filter-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-filter-action-6-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-filter-action-6-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-filter-action-7-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-filter-action-7-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-filter-action-8-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-filter-action-8-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-filter-action-9-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-filter-action-9-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-listview-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-listview-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-select-dialog-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-select-dialog-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-select-dialog-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-select-dialog-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-view-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-view-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-view-action-10-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-view-action-10-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-view-action-11-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-view-action-11-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-view-action-12-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-view-action-12-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-view-action-13-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-view-action-13-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-view-action-14-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-view-action-14-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-view-action-15-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-view-action-15-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-view-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-view-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-view-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-view-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-view-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-view-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-view-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-view-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-view-action-6-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-view-action-6-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-view-action-7-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-view-action-7-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-view-action-8-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-view-action-8-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defect-view-action-9-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defect-view-action-9-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defects-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defects-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defects-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defects-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defects-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defects-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defects-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defects-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'defects-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'defects-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'editor-drawio-dialog-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'editor-drawio-dialog-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'editor-drawio-dialog-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'editor-drawio-dialog-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-filter-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-filter-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-filter-action-10-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-filter-action-10-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-filter-action-11-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-filter-action-11-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-filter-action-12-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-filter-action-12-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-filter-action-13-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-filter-action-13-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-filter-action-14-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-filter-action-14-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-filter-action-15-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-filter-action-15-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-filter-action-16-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-filter-action-16-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-filter-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-filter-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-filter-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-filter-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-filter-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-filter-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-filter-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-filter-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-filter-action-6-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-filter-action-6-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-filter-action-7-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-filter-action-7-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-filter-action-8-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-filter-action-8-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-filter-action-9-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-filter-action-9-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-listview-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-listview-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-listview-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-listview-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-listview-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-listview-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-listview-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-listview-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-listview-copy-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-listview-copy-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-view-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-view-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-view-action-10-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-view-action-10-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-view-action-11-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-view-action-11-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-view-action-12-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-view-action-12-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-view-action-13-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-view-action-13-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-view-action-14-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-view-action-14-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-view-action-15-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-view-action-15-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-view-action-16-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-view-action-16-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-view-action-17-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-view-action-17-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-view-action-18-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-view-action-18-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-view-action-19-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-view-action-19-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-view-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-view-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-view-action-20-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-view-action-20-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-view-action-21-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-view-action-21-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-view-action-22-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-view-action-22-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-view-action-23-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-view-action-23-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-view-action-24-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-view-action-24-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-view-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-view-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-view-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-view-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-view-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-view-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-view-action-6-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-view-action-6-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-view-action-7-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-view-action-7-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-view-action-8-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-view-action-8-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'feedback-view-action-9-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'feedback-view-action-9-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-feedback-derive-from-anchor-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-feedback-derive-from-anchor-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-feedback-mark-processed-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-feedback-mark-processed-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-reference-dialog-clear-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-reference-dialog-clear-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-reference-dialog-cancel-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-reference-dialog-cancel-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-reference-dialog-apply-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-reference-dialog-apply-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ideen-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ideen-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ideen-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ideen-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ideen-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ideen-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ideen-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ideen-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilenstein-filter-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilenstein-filter-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilenstein-filter-action-10-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilenstein-filter-action-10-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilenstein-filter-action-11-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilenstein-filter-action-11-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilenstein-filter-action-12-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilenstein-filter-action-12-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilenstein-filter-action-13-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilenstein-filter-action-13-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilenstein-filter-action-14-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilenstein-filter-action-14-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilenstein-filter-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilenstein-filter-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilenstein-filter-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilenstein-filter-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilenstein-filter-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilenstein-filter-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilenstein-filter-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilenstein-filter-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilenstein-filter-action-6-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilenstein-filter-action-6-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilenstein-filter-action-7-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilenstein-filter-action-7-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilenstein-filter-action-8-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilenstein-filter-action-8-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilenstein-filter-action-9-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilenstein-filter-action-9-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilenstein-listview-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilenstein-listview-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilenstein-saveedit-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilenstein-saveedit-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilenstein-saveedit-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilenstein-saveedit-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilenstein-saveedit-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilenstein-saveedit-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilenstein-saveedit-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilenstein-saveedit-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilenstein-saveedit-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilenstein-saveedit-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilenstein-saveedit-action-6-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilenstein-saveedit-action-6-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilenstein-saveedit-action-7-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilenstein-saveedit-action-7-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilenstein-view-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilenstein-view-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilenstein-view-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilenstein-view-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilenstein-view-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilenstein-view-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'meilensteine-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'meilensteine-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'neoversion-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'neoversion-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'neoversion-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'neoversion-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'neoversion-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'neoversion-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'neoversion-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'neoversion-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'neoversion-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'neoversion-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'neoversion-action-6-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'neoversion-action-6-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'neoversion-action-7-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'neoversion-action-7-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'open-jira-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'open-jira-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'process-model-tree-select-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'process-model-tree-select-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'process-model-viewer-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'process-model-viewer-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'process-model-viewer-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'process-model-viewer-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'process-model-viewer-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'process-model-viewer-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'process-model-viewer-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'process-model-viewer-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'process-model-viewer-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'process-model-viewer-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'process-model-viewer-action-6-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'process-model-viewer-action-6-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'process-tree-node-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'process-tree-node-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-10-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-10-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-11-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-11-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-12-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-12-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-13-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-13-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-14-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-14-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-15-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-15-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-16-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-16-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-17-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-17-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-18-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-18-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-19-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-19-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-20-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-20-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-21-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-21-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-22-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-22-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-23-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-23-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-24-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-24-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-25-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-25-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-26-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-26-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-27-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-27-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-28-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-28-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-29-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-29-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-30-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-30-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-6-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-6-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-7-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-7-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-8-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-8-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'prozessmodell-action-9-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'prozessmodell-action-9-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'referenzmodell-field-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'referenzmodell-field-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'referenzmodell-field-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'referenzmodell-field-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'plaintext-editor-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'plaintext-editor-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'release-deadline-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'release-deadline-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'release-deadline-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'release-deadline-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'releases-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'releases-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'releases-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'releases-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'releases-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'releases-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'releases-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'releases-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'releases-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'releases-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'releases-action-6-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'releases-action-6-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'releases-action-7-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'releases-action-7-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'releases-action-8-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'releases-action-8-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'services-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'services-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'services-action-10-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'services-action-10-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'services-action-11-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'services-action-11-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'services-action-12-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'services-action-12-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'services-action-13-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'services-action-13-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'services-action-14-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'services-action-14-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'services-action-15-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'services-action-15-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'services-action-16-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'services-action-16-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'services-action-17-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'services-action-17-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'services-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'services-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'services-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'services-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'services-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'services-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'services-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'services-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'services-action-6-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'services-action-6-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'services-action-7-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'services-action-7-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'services-action-8-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'services-action-8-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'services-action-9-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'services-action-9-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-10-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-10-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-11-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-11-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-12-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-12-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-13-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-13-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-14-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-14-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-15-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-15-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-16-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-16-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-17-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-17-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-18-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-18-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-19-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-19-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-20-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-20-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-21-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-21-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-22-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-22-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-23-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-23-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-24-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-24-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-25-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-25-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-26-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-26-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-27-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-27-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-28-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-28-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-6-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-6-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-7-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-7-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-8-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-8-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-contentview-confluence-action-9-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-contentview-confluence-action-9-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-10-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-10-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-11-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-11-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-12-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-12-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-13-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-13-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-14-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-14-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-15-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-15-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-16-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-16-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-17-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-17-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-18-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-18-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-19-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-19-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-20-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-20-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-21-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-21-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-22-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-22-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-23-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-23-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-24-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-24-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-25-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-25-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-26-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-26-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-6-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-6-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-7-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-7-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-8-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-8-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-action-9-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-action-9-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-action-10-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-action-10-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-action-11-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-action-11-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-action-12-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-action-12-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-action-13-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-action-13-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-action-14-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-action-14-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-action-15-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-action-15-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-action-16-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-action-16-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-action-17-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-action-17-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-action-18-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-action-18-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-action-19-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-action-19-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-action-20-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-action-20-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-action-21-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-action-21-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-action-6-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-action-6-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-action-7-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-action-7-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-action-8-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-action-8-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-action-9-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-action-9-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-drawio-accordion-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-drawio-accordion-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-drawio-accordion-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-drawio-accordion-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-drawio-accordion-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-drawio-accordion-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-export-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-export-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-export-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-export-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-filter-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-filter-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-filter-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-filter-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-filter-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-filter-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-filter-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-filter-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-svg-viewer-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-svg-viewer-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-svg-viewer-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-svg-viewer-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-svg-viewer-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-svg-viewer-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-svg-viewer-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-svg-viewer-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-svg-viewer-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-svg-viewer-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-svg-viewer-action-6-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-svg-viewer-action-6-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-svg-viewer-action-7-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-svg-viewer-action-7-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-svg-viewer-action-8-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-svg-viewer-action-8-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-svg-viewer-action-9-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-svg-viewer-action-9-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-toc-dialog-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-toc-dialog-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-open-reference-model-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-open-reference-model-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'todo-checklist-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'todo-checklist-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'todo-checklist-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'todo-checklist-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'todo-checklist-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'todo-checklist-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'todos-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'todos-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'todos-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'todos-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'todos-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'todos-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'todos-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'todos-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'todos-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'todos-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'todos-action-6-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'todos-action-6-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'todos-action-7-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'todos-action-7-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-card-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-card-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-10-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-10-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-11-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-11-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-12-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-12-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-13-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-13-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-14-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-14-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-15-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-15-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-16-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-16-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-17-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-17-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-18-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-18-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-19-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-19-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-20-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-20-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-21-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-21-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-22-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-22-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-23-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-23-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-24-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-24-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-25-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-25-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-26-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-26-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-27-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-27-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-28-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-28-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-29-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-29-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-30-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-30-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-6-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-6-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-7-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-7-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-8-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-8-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-filter-action-9-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-filter-action-9-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-listview-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-listview-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-listview-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-listview-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-relations-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-relations-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-relations-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-relations-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-relations-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-relations-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-relations-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-relations-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-relations-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-relations-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-relations-action-6-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-relations-action-6-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-saveedit-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-saveedit-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-saveedit-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-saveedit-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-saveedit-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-saveedit-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-saveedit-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-saveedit-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-saveedit-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-saveedit-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-saveedit-action-6-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-saveedit-action-6-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-saveedit-action-7-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-saveedit-action-7-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-select-dialog-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-select-dialog-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-select-dialog-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-select-dialog-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-select-dialog-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-select-dialog-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-types-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-types-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-types-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-types-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-types-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-types-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-types-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-types-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-types-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-types-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-types-action-6-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-types-action-6-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-types-action-7-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-types-action-7-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-view-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-view-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-view-action-10-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-view-action-10-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-view-action-11-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-view-action-11-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-view-action-12-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-view-action-12-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-view-action-13-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-view-action-13-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-view-action-14-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-view-action-14-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-view-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-view-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-view-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-view-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-view-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-view-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-view-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-view-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-view-action-6-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-view-action-6-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-view-action-7-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-view-action-7-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-view-action-8-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-view-action-8-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziel-view-action-9-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziel-view-action-9-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziele-action-1-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziele-action-1-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziele-action-2-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziele-action-2-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziele-action-3-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziele-action-3-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziele-action-4-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziele-action-4-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziele-action-5-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziele-action-5-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziele-action-6-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziele-action-6-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziele-action-7-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziele-action-7-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'ziele-action-8-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'ziele-action-8-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-header-search-toggle-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-header-search-toggle-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-spezifikationen-list-mode-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-spezifikationen-list-mode-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-spezifikationen-tree-export-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-spezifikationen-tree-export-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-spezifikationen-tree-export-cancel-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-spezifikationen-tree-export-cancel-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-spezifikationen-tree-export-confirm-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-spezifikationen-tree-export-confirm-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-toggle-filter-by-list-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-toggle-filter-by-list-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-clone-cancel-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-clone-cancel-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-clone-confirm-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-clone-confirm-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-edit-cancel-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-edit-cancel-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-edit-save-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-edit-save-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-feedback-derivation-cancel-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-feedback-derivation-cancel-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-feedback-derivation-confirm-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-feedback-derivation-confirm-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-reference-apply-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-reference-apply-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-reference-cancel-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-reference-cancel-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-reference-clear-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-reference-clear-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-search-options-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-search-options-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-search-options-close-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-search-options-close-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-clear-query-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-filter-clear-query-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-spec-summary-open-viewer-link-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-spec-summary-open-viewer-link-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-side-open-requirement-viewer-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-side-open-requirement-viewer-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-page-list-mode-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-page-list-mode-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-page-tree-export-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-page-tree-export-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-page-tree-export-cancel-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-page-tree-export-cancel-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Button-Action automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-page-tree-export-confirm-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-page-tree-export-confirm-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Dynamisches Button-Pattern als Action registriert, bitte validieren.
  {
    trigger: '`idee-filter-active-status-${item}`',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: '`idee-filter-active-status-${item}`',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Dynamisches Button-Pattern als Action registriert, bitte validieren.
  {
    trigger: '`idee-listview-select-${props.row.id}`',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: '`idee-listview-select-${props.row.id}`',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Dynamisches Button-Pattern als Action registriert, bitte validieren.
  {
    trigger: '`anforderungsviewer-requirement-button-${row.id}`',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: '`anforderungsviewer-requirement-button-${row.id}`',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Dynamisches Button-Pattern als Action registriert, bitte validieren.
  {
    trigger: '`spezifikation-listview-open-confluence-${props.row.id}`',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: '`spezifikation-listview-open-confluence-${props.row.id}`',
        visible: true,
      },
    },
  },

]
