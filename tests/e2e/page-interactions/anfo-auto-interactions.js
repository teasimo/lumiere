// Diese Datei wurde automatisiert erzeugt.
// Jeder Eintrag ist ein ungepruefter KI-Vorschlag und muss fachlich validiert werden.

export const anfoAutoInteractions = [
  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-detail-container',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-detail-container',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-detail-status-label',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-detail-status-label',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderung-detail-status-list',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderung-detail-status-list',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-clone-action-group',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-clone-action-group',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-clone-cancel-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-clone-cancel-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-clone-confirm-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-clone-confirm-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-clone-dialog',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-clone-dialog',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-clone-include-requirements-checkbox',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-clone-include-requirements-checkbox',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-clone-insert-mode-group',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-clone-insert-mode-group',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-clone-multiselect-checkbox',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-clone-multiselect-checkbox',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-clone-source-select',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-clone-source-select',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-clone-source-viewer',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-clone-source-viewer',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-create-abschnitt-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-create-abschnitt-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-create-anforderung-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-create-anforderung-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-create-kapitel-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-create-kapitel-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-edit-spezifikation-cancel-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-edit-spezifikation-cancel-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-edit-spezifikation-dialog',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-edit-spezifikation-dialog',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-edit-spezifikation-info-input',
    verb: 'fill',
    defaultOutcome: 'valueUpdated',
    outcomes: {
      valueUpdated: {
        target: 'anforderungsviewer-edit-spezifikation-info-input',
        valueFromParam: 'value',
      },
      ready: {
        target: 'anforderungsviewer-edit-spezifikation-info-input',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-edit-spezifikation-querschnitt-input',
    verb: 'fill',
    defaultOutcome: 'valueUpdated',
    outcomes: {
      valueUpdated: {
        target: 'anforderungsviewer-edit-spezifikation-querschnitt-input',
        valueFromParam: 'value',
      },
      ready: {
        target: 'anforderungsviewer-edit-spezifikation-querschnitt-input',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-edit-spezifikation-reference-field',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-edit-spezifikation-reference-field',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-edit-spezifikation-save-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-edit-spezifikation-save-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-edit-spezifikation-title-input',
    verb: 'fill',
    defaultOutcome: 'valueUpdated',
    outcomes: {
      valueUpdated: {
        target: 'anforderungsviewer-edit-spezifikation-title-input',
        valueFromParam: 'value',
      },
      ready: {
        target: 'anforderungsviewer-edit-spezifikation-title-input',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-edit-spezifikation-typ-select',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-edit-spezifikation-typ-select',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-edit-spezifikation-wip-input',
    verb: 'fill',
    defaultOutcome: 'valueUpdated',
    outcomes: {
      valueUpdated: {
        target: 'anforderungsviewer-edit-spezifikation-wip-input',
        valueFromParam: 'value',
      },
      ready: {
        target: 'anforderungsviewer-edit-spezifikation-wip-input',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-feedback-derivation-cancel-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-feedback-derivation-cancel-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-feedback-derivation-confirm-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-feedback-derivation-confirm-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-feedback-derivation-dialog',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-feedback-derivation-dialog',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-feedback-derivation-remember-checkbox',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-feedback-derivation-remember-checkbox',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-header-actions',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-header-actions',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-header-actions-component',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-header-actions-component',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-header-container',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-header-container',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-header-search',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-header-search',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-header-search-select',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-header-search-select',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-header-search-title',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-header-search-title',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-header-search-toggle-button',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-header-search-toggle-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-left-panel-tabs',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-left-panel-tabs',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-left-tab-anforderungen',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-left-tab-anforderungen',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-left-tab-anforderungen-panel',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-left-tab-anforderungen-panel',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-left-tab-spezifikationen-panel',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-left-tab-spezifikationen-panel',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-container',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-container',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-local-viewer-container',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-local-viewer-container',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-menu-edit-spezifikation-item',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-menu-edit-spezifikation-item',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-menu-filter-anforderungen-item',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-menu-filter-anforderungen-item',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-menu-open-clone-dialog-item',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-menu-open-clone-dialog-item',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-menu-open-confluence-item',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-menu-open-confluence-item',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-menu-open-drawio-item',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-menu-open-drawio-item',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-menu-settings-item',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-menu-settings-item',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-more-menu-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-more-menu-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-open-swimlane-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-open-swimlane-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-open-toc-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-open-toc-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-plaintext-editor-container',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-plaintext-editor-container',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-plaintext-reference-hover',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-plaintext-reference-hover',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-plaintext-reference-hover-open-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-plaintext-reference-hover-open-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-plaintext-render-state',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-plaintext-render-state',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-plaintext-restore-info-dialog',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-plaintext-restore-info-dialog',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-plaintext-restore-info-ok-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-plaintext-restore-info-ok-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-reference-apply-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-reference-apply-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-reference-cancel-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-reference-cancel-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-reference-clear-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-reference-clear-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-reference-dialog',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-reference-dialog',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-reference-tree',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-reference-tree',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-right-spezifikation-details',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-right-spezifikation-details',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-right-spezifikation-empty-state',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-right-spezifikation-empty-state',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-save-plaintext-anchor-idee-select',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-save-plaintext-anchor-idee-select',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-settings-plaintext-item',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-settings-plaintext-item',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-settings-plaintext-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-settings-plaintext-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-settings-show-all-planned-changes-item',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-settings-show-all-planned-changes-item',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-settings-show-all-planned-changes-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-settings-show-all-planned-changes-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-settings-plaintext-grammar-diagnostics-item',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-settings-plaintext-grammar-diagnostics-item',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-settings-plaintext-grammar-diagnostics-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-settings-plaintext-grammar-diagnostics-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-settings-plaintext-autocomplete-item',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-settings-plaintext-autocomplete-item',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-settings-plaintext-autocomplete-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-settings-plaintext-autocomplete-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-settings-show-obsolete-item',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-settings-show-obsolete-item',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-settings-show-obsolete-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-settings-show-obsolete-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-settings-show-planned-obsolete-item',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-settings-show-planned-obsolete-item',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-settings-show-planned-obsolete-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-settings-show-planned-obsolete-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-settings-soft-delete-item',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-settings-soft-delete-item',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-settings-soft-delete-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-settings-soft-delete-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-settings-toggle-remarks-item',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-settings-toggle-remarks-item',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-spezifikationen-filter',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-spezifikationen-filter',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-spezifikationen-list-controls',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-spezifikationen-list-controls',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-spezifikationen-list-mode-toggle',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-spezifikationen-list-mode-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-spezifikationen-tree-export-button',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-spezifikationen-tree-export-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-spezifikationen-tree-export-cancel-button',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-spezifikationen-tree-export-cancel-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-spezifikationen-tree-export-confirm-button',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-spezifikationen-tree-export-confirm-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-spezifikationen-tree-export-dialog',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-spezifikationen-tree-export-dialog',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-spezifikationen-tree-export-only-filled-toggle',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-spezifikationen-tree-export-only-filled-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-spezifikationen-treeview',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-spezifikationen-treeview',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-svg-viewer',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-svg-viewer',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-toc-dialog',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-toc-dialog',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-toc-show-all-headings-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-toc-show-all-headings-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-toggle-all-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-toggle-all-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-toggle-filter-by-list-button',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-toggle-filter-by-list-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-toggle-view-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-toggle-view-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-clone-cancel-button',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-clone-cancel-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-clone-confirm-button',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-clone-confirm-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-clone-dialog',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-clone-dialog',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-clone-include-requirements-checkbox',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-clone-include-requirements-checkbox',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-clone-insert-mode-group',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-clone-insert-mode-group',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-clone-source-select',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-clone-source-select',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-edit-cancel-button',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-edit-cancel-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-edit-info-input',
    verb: 'fill',
    defaultOutcome: 'valueUpdated',
    outcomes: {
      valueUpdated: {
        target: 'anforderungsviewer-viewer-edit-info-input',
        valueFromParam: 'value',
      },
      ready: {
        target: 'anforderungsviewer-viewer-edit-info-input',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-edit-querschnitt-input',
    verb: 'fill',
    defaultOutcome: 'valueUpdated',
    outcomes: {
      valueUpdated: {
        target: 'anforderungsviewer-viewer-edit-querschnitt-input',
        valueFromParam: 'value',
      },
      ready: {
        target: 'anforderungsviewer-viewer-edit-querschnitt-input',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-edit-reference-field',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-edit-reference-field',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-edit-save-button',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-edit-save-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-edit-spezifikation-dialog',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-edit-spezifikation-dialog',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-edit-title-input',
    verb: 'fill',
    defaultOutcome: 'valueUpdated',
    outcomes: {
      valueUpdated: {
        target: 'anforderungsviewer-viewer-edit-title-input',
        valueFromParam: 'value',
      },
      ready: {
        target: 'anforderungsviewer-viewer-edit-title-input',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-edit-typ-select',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-edit-typ-select',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-edit-wip-input',
    verb: 'fill',
    defaultOutcome: 'valueUpdated',
    outcomes: {
      valueUpdated: {
        target: 'anforderungsviewer-viewer-edit-wip-input',
        valueFromParam: 'value',
      },
      ready: {
        target: 'anforderungsviewer-viewer-edit-wip-input',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-feedback-derivation-cancel-button',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-feedback-derivation-cancel-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-feedback-derivation-confirm-button',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-feedback-derivation-confirm-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-feedback-derivation-dialog',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-feedback-derivation-dialog',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-feedback-derivation-remember-checkbox',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-feedback-derivation-remember-checkbox',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-header-actions-component',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-header-actions-component',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-header-container',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-header-container',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-header-search',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-header-search',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-reference-apply-button',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-reference-apply-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-reference-cancel-button',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-reference-cancel-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-reference-clear-button',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-reference-clear-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-reference-dialog',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-reference-dialog',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-reference-tree',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-reference-tree',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-search-field-abschnitt',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-search-field-abschnitt',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-search-field-anforderung',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-search-field-anforderung',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-search-field-id',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-search-field-id',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-search-field-info',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-search-field-info',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-search-field-kapitel',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-search-field-kapitel',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-search-field-regeln',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-search-field-regeln',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-search-field-wip',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-search-field-wip',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-search-input',
    verb: 'fill',
    defaultOutcome: 'valueUpdated',
    outcomes: {
      valueUpdated: {
        target: 'anforderungsviewer-viewer-search-input',
        valueFromParam: 'value',
      },
      ready: {
        target: 'anforderungsviewer-viewer-search-input',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-search-match-mode-select',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-search-match-mode-select',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-search-options-button',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-search-options-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-search-options-close-button',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-search-options-close-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-search-options-dialog',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-search-options-dialog',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-search-use-regex-checkbox',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-search-use-regex-checkbox',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-svg-viewer',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-svg-viewer',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-viewer-toc-dialog',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-viewer-toc-dialog',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'app-confirm-dialog',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'app-confirm-dialog',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-active-reference-field',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-filter-active-reference-field',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-apply-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-filter-apply-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-clear-owner-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-filter-clear-owner-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-clear-query-button',
    verb: 'fill',
    defaultOutcome: 'valueUpdated',
    outcomes: {
      valueUpdated: {
        target: 'idee-filter-clear-query-button',
        valueFromParam: 'value',
      },
      ready: {
        target: 'idee-filter-clear-query-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-clear-unassigned-only-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-filter-clear-unassigned-only-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-clear-ziel-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-filter-clear-ziel-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-container',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-filter-container',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-owner-select',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-filter-owner-select',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-panel',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-filter-panel',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-query-input',
    verb: 'fill',
    defaultOutcome: 'valueUpdated',
    outcomes: {
      valueUpdated: {
        target: 'idee-filter-query-input',
        valueFromParam: 'value',
      },
      ready: {
        target: 'idee-filter-query-input',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-quick-angenommen-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-filter-quick-angenommen-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-quick-entscheidungsfaehig-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-filter-quick-entscheidungsfaehig-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-quick-my-owner-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-filter-quick-my-owner-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-quick-neu-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-filter-quick-neu-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-reference-apply-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-filter-reference-apply-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-reference-cancel-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-filter-reference-cancel-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-reference-clear-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-filter-reference-clear-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-reference-dialog',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-filter-reference-dialog',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-reference-field',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-filter-reference-field',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-reference-mode-select',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-filter-reference-mode-select',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-reference-tree',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-filter-reference-tree',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-reset-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-filter-reset-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-status-select',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-filter-status-select',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-toggle-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-filter-toggle-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-filter-ziel-multiselect',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-filter-ziel-multiselect',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-listview-container',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-listview-container',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-listview-table',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-listview-table',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-select-dialog-close-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-select-dialog-close-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-select-dialog-filter',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-select-dialog-filter',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-select-dialog-list',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-select-dialog-list',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-select-dialog-modal',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-select-dialog-modal',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-select-dialog-object-select',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-select-dialog-object-select',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-select-dialog-open-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-select-dialog-open-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-spec-summary-id-cell',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-spec-summary-id-cell',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-spec-summary-minus-abschnitt-count-cell',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-spec-summary-minus-abschnitt-count-cell',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-spec-summary-minus-count-cell',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-spec-summary-minus-count-cell',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-spec-summary-move-minus-count-cell',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-spec-summary-move-minus-count-cell',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-spec-summary-move-plus-count-cell',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-spec-summary-move-plus-count-cell',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-spec-summary-open-viewer-link-button',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-spec-summary-open-viewer-link-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-spec-summary-plus-abschnitt-count-cell',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-spec-summary-plus-abschnitt-count-cell',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-spec-summary-plus-count-cell',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-spec-summary-plus-count-cell',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-spec-summary-row',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-spec-summary-row',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-spec-summary-status-target-cell',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-spec-summary-status-target-cell',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-spec-summary-title-cell',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-spec-summary-title-cell',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-change-plan-anforderung-id-cell',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-change-plan-anforderung-id-cell',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-change-plan-anforderung-text-cell',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-change-plan-anforderung-text-cell',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-change-plan-details-toggle-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-change-plan-details-toggle-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-change-plan-move-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-change-plan-move-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-change-plan-move-target-select',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-change-plan-move-target-select',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-change-plan-row',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-change-plan-row',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-change-plan-row-checkbox',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-change-plan-row-checkbox',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-change-plan-select-cell',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-change-plan-select-cell',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-change-plan-spec-cell',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-change-plan-spec-cell',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-change-plan-table',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-change-plan-table',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-change-plan-vorhaben-cell',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-change-plan-vorhaben-cell',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-container',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-container',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-mobile-cancel-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-mobile-cancel-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-mobile-edit-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-mobile-edit-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-mobile-save-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-mobile-save-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-owner-select',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-owner-select',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-reference-field',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-reference-field',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-side-copy-link-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-side-copy-link-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-side-feedback-anchor-link-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-side-feedback-anchor-link-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-side-open-requirement-viewer-button',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-side-open-requirement-viewer-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-side-panel-container',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-side-panel-container',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-spec-summary-table',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-spec-summary-table',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-status-abgelehnt-node',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-status-abgelehnt-node',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-status-diagram-svg',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-status-diagram-svg',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-status-entscheidungsfaehig-node',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-status-entscheidungsfaehig-node',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-status-neu-node',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-status-neu-node',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-status-obsolet-node',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-status-obsolet-node',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-status-select',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-status-select',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-status-umgesetzt-node',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-status-umgesetzt-node',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'idee-view-ziel-select-dialog',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'idee-view-ziel-select-dialog',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-add-dialog',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-add-dialog',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-add-form',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-add-form',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-filter-container',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-filter-container',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-filter-reference-dialog',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-filter-reference-dialog',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-filter-reference-field-container',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-filter-reference-field-container',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-filter-reference-tree',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-filter-reference-tree',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-form-referenzmodell-dialog',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-form-referenzmodell-dialog',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-form-referenzmodell-tree',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-form-referenzmodell-tree',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-listview-count',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-listview-count',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-listview-horizontal-scroll',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-listview-horizontal-scroll',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-listview-scroll-area',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-listview-scroll-area',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-listview-table',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-listview-table',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-page-create-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-page-create-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-page-create-container',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-page-create-container',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-page-detail-view',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-page-detail-view',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-page-drawer',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-page-drawer',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-page-empty-state',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-page-empty-state',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-page-filter-component',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-page-filter-component',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-page-list-column',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-page-list-column',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-page-list-controls',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-page-list-controls',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-page-list-mode-toggle',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-page-list-mode-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-page-listview',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-page-listview',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-page-tree-export-button',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-page-tree-export-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-page-tree-export-cancel-button',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-page-tree-export-cancel-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-page-tree-export-confirm-button',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-page-tree-export-confirm-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-page-tree-export-dialog',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-page-tree-export-dialog',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-page-tree-export-only-filled-toggle',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-page-tree-export-only-filled-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-page-treeview',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-page-treeview',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-content-container',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-content-container',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-copy-link-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-copy-link-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-edit-cancel-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-edit-cancel-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-edit-dialog',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-edit-dialog',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-edit-form',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-edit-form',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-edit-lebenszyklus-select',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-edit-lebenszyklus-select',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-edit-save-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-edit-save-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-header-container',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-header-container',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-menu-plan-abgestimmt-item',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-menu-plan-abgestimmt-item',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-menu-plan-obsolet-item',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-menu-plan-obsolet-item',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-menu-remove-plan-item',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-menu-remove-plan-item',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-menu-soft-delete-item',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-menu-soft-delete-item',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-more-menu-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-more-menu-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-open-edit-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-open-edit-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-open-reference-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-open-reference-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-reference-cancel-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-reference-cancel-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-reference-clear-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-reference-clear-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-reference-dialog',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-reference-dialog',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-reference-save-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-reference-save-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-reference-tree',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-reference-tree',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-refresh-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-refresh-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-search-select',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-search-select',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-show-todo-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-show-todo-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-soft-delete-cancel-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-soft-delete-cancel-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-soft-delete-confirm-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-soft-delete-confirm-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-soft-delete-dialog',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-soft-delete-dialog',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-source-idea-label',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-source-idea-label',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-source-idea-open-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-source-idea-open-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-title',
    verb: 'read',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-title',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-view-toggle-search-button',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-view-toggle-search-button',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'anforderungsviewer-spezifikationen-tree-export-only-filled-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'anforderungsviewer-spezifikationen-tree-export-only-filled-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'components-anforderung-select-dialog-action-1-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'components-anforderung-select-dialog-action-1-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'components-defect-filter-action-1-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'components-defect-filter-action-1-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'components-defect-filter-action-2-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'components-defect-filter-action-2-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'components-feedback-view-action-1-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'components-feedback-view-action-1-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'components-meilenstein-saveedit-action-1-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'components-meilenstein-saveedit-action-1-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'components-spezifikation-contentview-confluence-action-1-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'components-spezifikation-contentview-confluence-action-1-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'components-spezifikation-contentview-confluence-action-2-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'components-spezifikation-contentview-confluence-action-2-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'components-spezifikation-contentview-confluence-action-3-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'components-spezifikation-contentview-confluence-action-3-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'components-spezifikation-contentview-confluence-action-4-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'components-spezifikation-contentview-confluence-action-4-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'components-spezifikation-contentview-confluence-action-5-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'components-spezifikation-contentview-confluence-action-5-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'components-spezifikation-contentview-confluence-action-6-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'components-spezifikation-contentview-confluence-action-6-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'components-spezifikation-svg-viewer-action-1-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'components-spezifikation-svg-viewer-action-1-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'components-ziel-view-action-1-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'components-ziel-view-action-1-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'pages-anforderungen-anforderungen-action-1-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'pages-anforderungen-anforderungen-action-1-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'pages-anforderungen-anforderungen-action-2-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'pages-anforderungen-anforderungen-action-2-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'pages-defects-defects-action-1-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'pages-defects-defects-action-1-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'pages-feedback-feedback-action-1-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'pages-feedback-feedback-action-1-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'pages-feedback-feedback-action-2-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'pages-feedback-feedback-action-2-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'pages-ideen-ideen-action-1-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'pages-ideen-ideen-action-1-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'pages-prozessmodell-action-1-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'pages-prozessmodell-action-1-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'pages-prozessmodell-action-2-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'pages-prozessmodell-action-2-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'pages-prozessmodell-action-3-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'pages-prozessmodell-action-3-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'pages-prozessmodell-action-4-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'pages-prozessmodell-action-4-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'pages-prozessmodell-action-5-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'pages-prozessmodell-action-5-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'pages-prozessmodell-action-6-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'pages-prozessmodell-action-6-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'pages-prozessmodell-action-7-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'pages-prozessmodell-action-7-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'pages-prozessmodell-action-8-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'pages-prozessmodell-action-8-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'pages-ziele-ziele-action-1-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'pages-ziele-ziele-action-1-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'pages-ziele-ziele-action-2-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'pages-ziele-ziele-action-2-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'pages-ziele-ziele-action-3-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'pages-ziele-ziele-action-3-toggle',
        visible: true,
      },
    },
  },

  // Ungepruefter KI-Vorschlag: Interaction automatisch abgeleitet, bitte validieren.
  {
    trigger: 'spezifikation-page-tree-export-only-filled-toggle',
    verb: 'action',
    defaultOutcome: 'ready',
    outcomes: {
      ready: {
        target: 'spezifikation-page-tree-export-only-filled-toggle',
        visible: true,
      },
    },
  },

]
