/**
 * App-specific Data Functions for Lunettes
 *
 * Central functions (randomNumber, uuid, etc.) are in scripts/generator/central-data-functions.mjs
 * Only app-specific functions should be here.
 */

export const dataFunctions = {
  // Add Lunettes-specific functions here as needed
  // Example:
  // ideaTitle() {
  //   return `Idea ${this.randomNumber()}`
  // },
}

export default dataFunctions
